# Sonorus Memory Index

- [Project: Sonorus Extension](project_sonorus.md) — Browser TTS extension (Chrome + Firefox + Safari), all 5 phases implemented, two-zip build output
- [Selection Filter](selection_filter.md) — `isSelectionSpeakable()` in content-selection.js gates the popup icon: skips input fields, code blocks, binary blobs, pure URL/email/number, emoji-only text
- [Command Reference](../../docs/commands.md) — every npm script and `safari.sh` flag, what each produces/deletes, per-browser load targets, CI. Note `npm run validate` deletes its own output.
